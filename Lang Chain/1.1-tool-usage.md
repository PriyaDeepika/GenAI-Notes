## Three Stages of Tool Usage

### 1. Tool Calling
The model decides whether a tool should be used.

### 2. Tool Execution
LangChain executes the Python function and returns the result.

### 3. Tool Faithfulness
The model decides whether to trust and use the tool result in its final response.

Important: A model can successfully call and execute a tool but still ignore the tool's output.
